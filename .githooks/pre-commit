#!/bin/sh
#
#set -eu
#
#repo_root=$(git rev-parse --show-toplevel) || exit 1
#
#if command -v pwsh >/dev/null 2>&1; then
#    exec pwsh -NoProfile -File "$repo_root/scripts/CheckCode.ps1"
#fi
#
#if command -v powershell.exe >/dev/null 2>&1; then
#    exec powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$repo_root/scripts/CheckCode.ps1"
#fi
#
#echo "PowerShell 7+ (pwsh) is required to run the pre-commit hook." >&2
#exit 1
